/*
*
* 구름 Level 정사각형의 개수
*/
/*
#include <iostream>

using namespace std;

int main() {
	
	long lCnt = 0;
	long lNum;

	cin >> lNum;

	// N^2 + (N-1)^2 + (N-2)^2 + ... + (N-M)^2 + 1
	for (long i = lNum; i > 0; --i) {
		lCnt += (i * i);
	}

	cout << lCnt;
	return 0;
}
*/

