/*
*
*	Programmers Lv1 평균구하기
*/
/*#include <iostream>
#include <string>
#include <vector>

using namespace std;

double solution(vector<int> arr);

auto main()->int {
    vector<int> intArr = { 1,2,3,4 };
	double result = solution(intArr);

    cout << result;
}

double solution(vector<int> arr) {
    double answer = 0;
    for (auto num : arr) {
        answer += num;
    }
    answer = answer / arr.size();

    return answer;
}
*/