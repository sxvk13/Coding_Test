/*
*
*	Programmers Lv1 하샤드 수
*/
/*#include <string>
#include <vector>
#include <iostream>

using namespace std;

bool solution(int x) {
    bool answer = true;

    string str = to_string(x);
    int iSum = 0;
    for (auto i : str) {
        iSum += (int)i - '0';
    }


    return answer = (x % iSum) == 0 ? true:false;
}
auto main()->int {

    int X;
    cin >> X;
    bool result = solution(X);

    cout << result;

}*/